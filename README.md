# File-Type-Identifier-Using-Magic-Numbers
File Type Identifier is a lightweight web application that identifies a file's actual type using its magic number (file signature) rather than relying solely on the file extension.
